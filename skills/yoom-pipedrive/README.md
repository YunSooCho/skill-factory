# Yoom Integration - Pipedrive

API-based integration with OAuth. 28 actions, 10 triggers.

## Install
```bash
pip install aiohttp requests flask
```

## Env
```bash
YOOM_PIPEDRIVE_BASE_URL=https://api.pipedrive.com
YOOM_PIPEDRIVE_AUTH_TOKEN=your_token
```