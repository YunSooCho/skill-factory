# Google ドキュメント - Yoom 연계

## 📄 基本情報
- **サービス名:** Google ドキュメント
- **URL:** https://lp.yoom.fun/apps/google-docs
- **カテゴリー:** 業務一般 (업무 일반)

---

## 🔄 フローボットオペレーション (APIアクション) - 5개

### API 액션 목록 (전체):

1. **文末にテキストを追加** - 문서 끝에 텍스트 추가
2. **値を置換** - 값 치환
3. **新しいドキュメントを作成する** - 새로운 문서 생성
4. **テキストを挿入** - 텍스트 삽입
5. **画像を追加** - 이미지 추가

---

## 🎯 フローボットトリガー - 0개

### 트리거 없음

---


## 📋 テンプレート - 6개

### テンプレート一覧:

1. **メールで縦書きのPDF資料が届いたら、AIでテキストを抽出しGoogleドキュメントに内容を転記する**
   - 内容: メールで縦書きのPDF資料が届いたら、AIでテキストを抽出し自動でGoogleドキュメントに内容を転記するフローです。PDFファイルから文字データを読み取り自動で内容を入力してくれるので、手入力の手間をなくし、ミスを防ぐことができます。

2. **フォームに入力された内容を用いて、Googleドキュメントの雇用契約書雛形からPDFを発行する**
   - 内容: フォームの内容を用いて、予め用意したGoogleドキュメントの雇用契約書雛形にフォーム内容を入力して、PDFを発行します。通常、雛形シートをコピーし、入力してPDFを発行するという流れになりますが、そのような手動操作が不要で、フォームの入力だけで書類発行が自動で行えます。

3. **フォームに入力された内容を用いて、Googleドキュメントの雇用契約書雛形からPDFを発行しDiscordに通知する**
   - 内容: Yoomフォームの内容を用いて、予め用意したGoogleドキュメントの雇用契約書雛形にフォーム内容を入力して、PDFを発行してDiscordに通知するフローです。通常、雛形シートをコピーし、入力してPDFを発行するという流れになりますが、そのような手動操作が不要で、フォームの入力だけで書類発行が自動で行えます。

4. **会議の内容を文字起こしし、Googleドキュメントに格納**
   - 内容: 入力フォームにアップロードした会議データの内容から音声を文字起こしし、Googleドキュメントで文字起こしデータを自動的に作成するフローボットです。ZoomやMeetなどのレコーディングデータなどの会議データをアップロードしてご利用ください。‍

5. **メールで添付書類を受信したらAIで読み取りデジタル化し、Googleドキュメントに転記する**
   - 内容: メールで添付書類を受信したらAIで読み取りデジタル化し、自動でGoogleドキュメントに転記するフローです。データをAIが自動で解析してくれるので、手入力の手間を省きデータの入力ミスを防ぎ、業務の効率を大幅に向上させることができます。

6. **Googleフォームの回答内容をGoogle ドキュメントに追加する**
   - 内容: Googleフォームの送信をトリガーに、回答内容をGoogle ドキュメントの末尾へ自動追加するフローです。手作業のコピー＆ペーストを省き、転記ミスを防ぎながら作業時間を短縮できます。


## ✅ Skill Factory 매칭
- **サービス:** Google ドキュメントがSkill Factory 스펙에 포함됨
- **yoom連携:** ◯ (확인됨)