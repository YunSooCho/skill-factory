# HEARTBEAT.md

# This heartbeat is now handled by cron job "Skill Factory Service Discovery"
# No manual heartbeat needed.

# The cron job runs automatically every 30 minutes in isolated session
# and performs: gap analysis → service discovery → URL validation → git push.